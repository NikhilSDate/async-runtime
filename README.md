# Rust Async Runtime

Simple asynchronous runtime written in Rust. Currently includes an Executor (with support for spawning and joining on tasks within other tasks) and Reactor, timers, and UDP support.  

## Features

- Thread pool executor
- UDP support
- Timers
- spawn/join
